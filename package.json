{
  "name": "forgeai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "python -m uvicorn backend.gateway.main:app --host 0.0.0.0 --port 8000 --reload",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "python run_local.py",
    "build": "cd frontend && npm install && npm run build",
    "start": "npm --prefix frontend run start"
  },
  "dependencies": {
    "next": "14.2.3"
  }
}
